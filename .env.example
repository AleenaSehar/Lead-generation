# The current prototype does not require environment variables.
# Future integrations must document variable names here using safe placeholder values.
#
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# AUTH_SECRET=replace-with-a-generated-development-secret
